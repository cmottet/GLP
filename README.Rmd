README

This package is the companion R package to the paper by Mottet, C. and Lam, H. "On Optimization over Tail Distributions". It is designed to solve program (24) using the generalized linear programming (GLP) procedure described in Algorith 1.

It can be installed in R using the command

install.packages("devtools")
devtools::install_github("cmottet/GLP")
